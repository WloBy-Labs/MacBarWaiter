# MacBarWaiter 1.1.0

换了新图标，和 WloBy 其他 App 统一版式。

- **新 App 图标**：青绿底 + 一条菜单栏（右侧图标实心、往左逐渐淡出，表示被系统挤掉
  看不见了）+ 朝下的箭头，下方 `WLOBY MB` 字标。底色特意跟 PrWaiter 的靛蓝、
  KeyboardWaiter 的石墨区分开
- 字标只在 128px 以上出现 —— 16/32px 下八个字符横排必然糊成一团，那些尺寸只画图形
- CI 的 `actions/upload-artifact` 升到 v6，消除 Node.js 20 弃用告警

App 功能没有改动。
